spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: file:../config-repo
          default-label: master
          clone-on-start: false

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info